<Window x:Class="Spider.Views.Windows.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Напоминание"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        Width="500"
        Height="350"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="ReminderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3d8b40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SnoozeButtonStyle" TargetType="Button" BasedOn="{StaticResource ReminderButtonStyle}">
            <Setter Property="Background" Value="#FF9800"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e68900"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#cc7a00"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Border Background="#1E1E1E"
            CornerRadius="12"
            BorderBrush="#4CAF50"
            BorderThickness="3"
            Padding="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.7"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Background="#4CAF50" Height="60">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="⏰" FontSize="32" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TitleTextBlock"
                               Text="Время для разминки!"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="30,20">
                <StackPanel>
                    <TextBlock x:Name="MessageTextBlock"
                               Text="Пришло время сделать разминку для глаз и тела."
                               TextWrapping="Wrap"
                               FontSize="15"
                               Foreground="#DDDDDD"
                               LineHeight="24"
                               TextAlignment="Left"/>
                    
                    <!-- Recommendations Box -->
                    <Border Background="#2D2D2D"
                            CornerRadius="8"
                            Padding="20"
                            Margin="0,20,0,0"
                            BorderBrush="#4CAF50"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="💡 Рекомендуемые упражнения:"
                                       FontSize="14"
                                       FontWeight="Bold"
                                       Foreground="#4CAF50"
                                       Margin="0,0,0,12"/>
                            
                            <StackPanel>
                                <TextBlock Text="👁️ Для глаз:" FontWeight="SemiBold" Foreground="White" FontSize="13" Margin="0,0,0,5"/>
                                <TextBlock Text="• Отведите взгляд от монитора на 20 секунд" Foreground="#CCCCCC" FontSize="12" Margin="15,0,0,3"/>
                                <TextBlock Text="• Посмотрите на удалённые объекты (более 6 метров)" Foreground="#CCCCCC" FontSize="12" Margin="15,0,0,3"/>
                                <TextBlock Text="• Закройте глаза и расслабьтесь на 10-15 секунд" Foreground="#CCCCCC" FontSize="12" Margin="15,0,0,10"/>
                                
                                <TextBlock Text="💪 Для тела:" FontWeight="SemiBold" Foreground="White" FontSize="13" Margin="0,5,0,5"/>
                                <TextBlock Text="• Сделайте наклоны и повороты головы" Foreground="#CCCCCC" FontSize="12" Margin="15,0,0,3"/>
                                <TextBlock Text="• Разомните плечи круговыми движениями" Foreground="#CCCCCC" FontSize="12" Margin="15,0,0,3"/>
                                <TextBlock Text="• Встаньте и немного походите по комнате" Foreground="#CCCCCC" FontSize="12" Margin="15,0,0,3"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons -->
            <Grid Grid.Row="2" Background="#252525" Height="70">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                    <Button x:Name="SnoozeButton"
                            Content="⏰ Напомнить через 5 мин"
                            Click="SnoozeButton_Click"
                            Style="{StaticResource SnoozeButtonStyle}"
                            Margin="0,0,15,0"
                            MinWidth="180"/>
                    
                    <Button x:Name="OkButton"
                            Content="✓ Хорошо"
                            Click="OkButton_Click"
                            Style="{StaticResource ReminderButtonStyle}"
                            MinWidth="120"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>