# WPF CRUD Приложение - Техническое задание и инструкция

## 📋 Техническое задание

### Вкладка 1: Категории и данные
- Список категорий с возможностью CRUD операций (создание, чтение, обновление, удаление)
- При клике на категорию открывается список данных категории
- Каждая запись данных содержит: название и значение
- Данные также поддерживают CRUD операции

### Вкладка 2: Команды
- Список команд с возможностью CRUD операций
- При клике на команду появляется:
  - Поле ввода аргументов
  - Окно для отображения консольных логов выполнения
  - Кнопка "Запустить команду"
- Команды можно добавлять, редактировать и удалять

### Вкладка 3: Docker проекты
- Список проектов с CRUD операциями
- Каждый проект содержит путь до docker-compose файла
- При клике на проект отображается список образов Docker
- Для каждого образа можно:
  - Запустить
  - Остановить
  - Пересобрать

### Вкладка 4: Скриншоты и запись видео
- Кнопка для создания скриншота:
  - Выбор области экрана
  - Редактирование перед сохранением (рисование, подписи)
  - Сохранение результата
- Кнопка для записи видео:
  - Выбор области экрана для записи
  - Запись видео выбранной области

---

## 🚀 Пошаговая инструкция по созданию приложения

### Шаг 1: Подготовка окружения

#### 1.1. Установка необходимого ПО
1. **Visual Studio 2022** (Community Edition или выше)
   - При установке выберите компонент ".NET desktop development"
   - Убедитесь, что установлен .NET 6.0 или .NET 7.0/8.0

2. **Дополнительные инструменты**
   - Git (для версионирования)
   - Docker Desktop (для тестирования функционала работы с Docker)

#### 1.2. Создание проекта
1. Откройте Visual Studio 2022
2. Выберите "Create a new project"
3. Найдите и выберите шаблон "WPF Application" (.NET)
4. Задайте параметры проекта:
   - Project name: `SpiderCrudApp`
   - Location: `F:\Code\spider`
   - Framework: .NET 6.0 или выше
5. Нажмите "Create"

---

### Шаг 2: Архитектура проекта

#### 2.1. Структура папок
Создайте следующую структуру папок в проекте:

```
SpiderCrudApp/
├── Models/              # Модели данных
│   ├── Category.cs
│   ├── CategoryData.cs
│   ├── Command.cs
│   ├── DockerProject.cs
│   └── DockerImage.cs
├── ViewModels/          # View Models (MVVM паттерн)
│   ├── MainViewModel.cs
│   ├── CategoriesViewModel.cs
│   ├── CommandsViewModel.cs
│   ├── DockerViewModel.cs
│   └── ScreenCaptureViewModel.cs
├── Views/               # Представления (UserControls для вкладок)
│   ├── CategoriesView.xaml
│   ├── CommandsView.xaml
│   ├── DockerView.xaml
│   └── ScreenCaptureView.xaml
├── Services/            # Бизнес-логика и сервисы
│   ├── DataService.cs
│   ├── CommandExecutor.cs
│   ├── DockerService.cs
│   └── ScreenCaptureService.cs
├── Data/                # Работа с данными
│   ├── DatabaseContext.cs
│   └── Repository.cs
├── Helpers/             # Вспомогательные классы
│   ├── RelayCommand.cs
│   └── ObservableObject.cs
└── MainWindow.xaml      # Главное окно приложения
```

#### 2.2. Выбор паттерна проектирования
**Рекомендуется использовать MVVM (Model-View-ViewModel):**
- **Model**: Классы данных и бизнес-логика
- **View**: XAML разметка интерфейса
- **ViewModel**: Связующее звено между View и Model

---

### Шаг 3: Хранение данных

#### 3.1. Выбор способа хранения
Для автономного приложения в одном EXE выберите один из вариантов:

**Вариант A: SQLite (Рекомендуется)**
- Легковесная встраиваемая база данных
- Хранится в одном файле
- Установите NuGet пакеты:
  - `Microsoft.EntityFrameworkCore.Sqlite`
  - `Microsoft.EntityFrameworkCore.Tools`

**Вариант B: LiteDB**
- NoSQL база данных для .NET
- Еще проще чем SQLite
- Установите NuGet пакет: `LiteDB`

**Вариант C: JSON файлы**
- Самый простой вариант
- Используйте `System.Text.Json` (встроенный в .NET)

#### 3.2. Создание моделей данных

Определите классы для каждой сущности:

**Category** (Категория):
- Id (int)
- Name (string)
- Collection<CategoryData>

**CategoryData** (Данные категории):
- Id (int)
- CategoryId (int)
- Name (string)
- Value (string)

**Command** (Команда):
- Id (int)
- Name (string)
- CommandText (string)
- DefaultArguments (string)

**DockerProject** (Docker проект):
- Id (int)
- Name (string)
- DockerComposePath (string)

**DockerImage** (Docker образ):
- ProjectId (int)
- ImageName (string)
- IsRunning (bool)

---

### Шаг 4: Реализация вкладок

#### 4.1. Главное окно (MainWindow.xaml)
Используйте `TabControl` для создания вкладок:
1. Добавьте `TabControl` в MainWindow
2. Создайте 4 `TabItem` для каждой функции
3. В каждый `TabItem` вставьте соответствующий UserControl

#### 4.2. Вкладка 1: Категории

**Элементы интерфейса:**
- **Левая панель**: ListBox со списком категорий
- **Кнопки управления категориями**: Добавить, Редактировать, Удалить
- **Правая панель**: DataGrid с данными выбранной категории
- **Кнопки управления данными**: Добавить, Редактировать, Удалить

**Логика работы:**
1. При загрузке подтянуть все категории из БД
2. При клике на категорию загрузить её данные
3. Реализовать диалоговые окна для добавления/редактирования
4. Обновлять ListBox и DataGrid после операций CRUD

**Диалоговые окна:**
- Создайте отдельное Window для добавления/редактирования категории
- Создайте отдельное Window для добавления/редактирования данных

#### 4.3. Вкладка 2: Команды

**Элементы интерфейса:**
- **Левая панель**: ListBox со списком команд
- **Кнопки управления**: Добавить, Редактировать, Удалить
- **Правая панель**:
  - TextBox для ввода аргументов
  - TextBox (многострочный) для отображения логов (ReadOnly)
  - Button "Запустить команду"
  - Button "Остановить" (для длительных команд)

**Логика работы:**
1. При выборе команды отобразить поле аргументов
2. При запуске команды:
   - Выполнить через `Process.Start()`
   - Перенаправить StandardOutput и StandardError в TextBox логов
   - Использовать асинхронное выполнение (`async/await`)
3. Обновлять логи в реальном времени

**Важные моменты:**
- Используйте `ProcessStartInfo` с перенаправлением вывода
- Обрабатывайте события `OutputDataReceived` и `ErrorDataReceived`
- Обновляйте UI через `Dispatcher.Invoke()` для потокобезопасности

#### 4.4. Вкладка 3: Docker проекты

**Элементы интерфейса:**
- **Левая панель**: ListBox со списком проектов
- **Кнопки управления проектами**: Добавить, Редактировать, Удалить
- **Правая панель**:
  - DataGrid со списком образов
  - Колонки: Имя образа, Статус, Действия
  - Кнопки для каждого образа: Запустить, Остановить, Пересобрать
  - TextBox для логов операций Docker

**Логика работы:**
1. При добавлении проекта использовать `OpenFileDialog` для выбора docker-compose.yml
2. При выборе проекта:
   - Выполнить `docker-compose ps` для получения списка образов
   - Парсить вывод и отобразить в DataGrid
3. Для управления образами выполнять команды:
   - Запуск: `docker-compose up -d [service]`
   - Остановка: `docker-compose stop [service]`
   - Пересборка: `docker-compose up -d --build [service]`
4. Логировать все операции в TextBox

**Важные моменты:**
- Установите рабочую директорию процесса на папку с docker-compose файлом
- Используйте `Process.Start()` для выполнения docker-compose команд
- Обрабатывайте ошибки (Docker не установлен, файл не найден и т.д.)

#### 4.5. Вкладка 4: Скриншоты и видео

**Элементы интерфейса:**
- Button "Создать скриншот"
- Button "Записать видео"
- Button "Открыть папку со скриншотами"
- ListBox с последними скриншотами/видео
- Image для предварительного просмотра

**Функционал скриншотов:**

1. **Выбор области:**
   - При нажатии кнопки свернуть главное окно
   - Показать полупрозрачное окно поверх всего экрана
   - Позволить мышью выделить область (прямоугольник)
   - Используйте `Graphics.CopyFromScreen()` для захвата

2. **Редактирование:**
   - Открыть захваченное изображение в редакторе
   - Реализовать инструменты:
     - Кисть (разные цвета и толщина)
     - Стрелки
     - Текст
     - Прямоугольники, эллипсы
     - Отмена/повтор действий
   - Кнопки: Сохранить, Отмена

3. **Сохранение:**
   - Использовать `SaveFileDialog`
   - Сохранять в форматах PNG, JPG
   - По умолчанию сохранять в папку `Screenshots` рядом с EXE

**Функционал записи видео:**

1. **Библиотеки для захвата видео:**
   - NuGet пакет: `SharpAvi` или `Accord.Video.FFMPEG`
   - Альтернатива: `ScreenRecorderLib`

2. **Процесс записи:**
   - Выбор области аналогично скриншоту
   - Запуск записи с заданным FPS (например, 30)
   - Индикатор записи (красная точка, таймер)
   - Кнопка остановки записи
   - Сохранение в формате AVI или MP4

**Важные моменты:**
- Запрашивайте разрешения Windows для захвата экрана
- Оптимизируйте производительность (не нагружайте CPU)
- Добавьте настройки качества записи

---

### Шаг 5: NuGet пакеты

Установите необходимые пакеты через NuGet Package Manager:

#### Обязательные:
```
# Для работы с базой данных (если выбрали SQLite)
Microsoft.EntityFrameworkCore.Sqlite
Microsoft.EntityFrameworkCore.Tools

# Для MVVM паттерна (опционально, можно реализовать самому)
CommunityToolkit.Mvvm
```

#### Для функционала скриншотов и видео:
```
# Для работы с изображениями
System.Drawing.Common

# Для записи видео
SharpAvi
# или
ScreenRecorderLib

# Для редактирования изображений (опционально)
ImageSharp
```

#### Для работы с Docker:
```
# Встроенные классы .NET достаточно
# Используйте System.Diagnostics.Process
```

---

### Шаг 6: Реализация CRUD операций

#### 6.1. Создание базового репозитория

Создайте generic репозиторий для работы с данными:
- Методы: `GetAll()`, `GetById(id)`, `Add(entity)`, `Update(entity)`, `Delete(id)`
- Используйте Entity Framework или прямые запросы к SQLite/LiteDB

#### 6.2. Реализация операций для каждой сущности

**Create (Создание):**
1. Открыть диалоговое окно с пустой формой
2. Валидация введенных данных
3. Сохранение в БД через репозиторий
4. Обновление списка в UI

**Read (Чтение):**
1. Загрузка данных из БД при инициализации
2. Привязка к ObservableCollection для автообновления UI

**Update (Обновление):**
1. Открыть диалоговое окно с заполненными данными
2. Позволить редактирование
3. Сохранить изменения в БД
4. Обновить UI

**Delete (Удаление):**
1. Показать подтверждение (MessageBox)
2. Удалить из БД
3. Удалить из коллекции UI

---

### Шаг 7: MVVM реализация

#### 7.1. Создание базового класса ObservableObject

Реализуйте `INotifyPropertyChanged`:
- Метод `OnPropertyChanged()`
- Используется для уведомления UI об изменениях

#### 7.2. Создание RelayCommand

Реализуйте `ICommand` для привязки команд к кнопкам:
- Принимает Action для выполнения
- Опциональный Predicate для `CanExecute`

#### 7.3. Создание ViewModel для каждой вкладки

**Свойства:**
- ObservableCollection для коллекций данных
- Свойства для выбранных элементов
- Свойства для полей ввода

**Команды:**
- AddCommand
- EditCommand
- DeleteCommand
- ExecuteCommand (для вкладки команд)
- И т.д.

**Методы:**
- LoadData()
- SaveData()
- ValidateInput()

---

### Шаг 8: Создание UI в XAML

#### 8.1. Дизайн главного окна

Используйте современные стили:
- Material Design (NuGet: `MaterialDesignThemes`)
- ModernWpf (NuGet: `ModernWpfUI`)
- Или создайте собственные стили

**Структура:**
```xml
<TabControl>
  <TabItem Header="Категории">
    <local:CategoriesView />
  </TabItem>
  <TabItem Header="Команды">
    <local:CommandsView />
  </TabItem>
  <TabItem Header="Docker">
    <local:DockerView />
  </TabItem>
  <TabItem Header="Скриншоты">
    <local:ScreenCaptureView />
  </TabItem>
</TabControl>
```

#### 8.2. Привязка данных (Data Binding)

Используйте привязку для связи ViewModel и View:
```xml
<ListBox ItemsSource="{Binding Categories}" 
         SelectedItem="{Binding SelectedCategory}" />
         
<Button Content="Добавить" 
        Command="{Binding AddCommand}" />
```

#### 8.3. Стилизация

Создайте ResourceDictionary с общими стилями:
- Цветовая схема
- Стили кнопок
- Стили ListBox/DataGrid
- Анимации и переходы

---

### Шаг 9: Сборка в один EXE

#### 9.1. Использование .NET 6+ с самостоятельным развертыванием

В файле проекта (.csproj) добавьте:
```xml
<PropertyGroup>
  <OutputType>WinExe</OutputType>
  <TargetFramework>net6.0-windows</TargetFramework>
  <UseWPF>true</UseWPF>
  
  <!-- Для публикации в один файл -->
  <PublishSingleFile>true</PublishSingleFile>
  <SelfContained>true</SelfContained>
  <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  <PublishReadyToRun>true</PublishReadyToRun>
  <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
</PropertyGroup>
```

#### 9.2. Публикация приложения

**Через Visual Studio:**
1. Правый клик на проект → Publish
2. Выберите "Folder" как target
3. Настройте параметры:
   - Target Runtime: win-x64
   - Deployment Mode: Self-contained
   - Produce single file: Yes
4. Нажмите "Publish"

**Через командную строку:**
```powershell
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
```

#### 9.3. Оптимизация размера

Добавьте в .csproj для уменьшения размера:
```xml
<PropertyGroup>
  <PublishTrimmed>true</PublishTrimmed>
  <TrimMode>link</TrimMode>
</PropertyGroup>
```

**Внимание:** Тестируйте после включения Trimming, может сломать reflection!

---

### Шаг 10: Обработка ошибок и логирование

#### 10.1. Try-Catch блоки

Оборачивайте критичные операции:
- Работа с файлами
- Выполнение команд
- Работа с Docker
- Захват экрана

#### 10.2. Логирование

Используйте:
- NuGet пакет `Serilog` для структурированного логирования
- Логируйте в файл рядом с приложением
- Настройте уровни логирования (Debug, Info, Warning, Error)

#### 10.3. Уведомления пользователя

- MessageBox для критичных ошибок
- StatusBar для информационных сообщений
- ProgressBar для длительных операций

---

### Шаг 11: Тестирование

#### 11.1. Функциональное тестирование

Проверьте каждую функцию:
- [ ] CRUD операции для категорий
- [ ] CRUD операции для данных категорий
- [ ] CRUD операции для команд
- [ ] Выполнение команд и отображение логов
- [ ] CRUD операции для Docker проектов
- [ ] Управление Docker образами
- [ ] Создание скриншотов
- [ ] Редактирование скриншотов
- [ ] Запись видео

#### 11.2. Тестирование edge cases

- Пустая база данных
- Некорректные пути к файлам
- Docker не установлен
- Отмена операций
- Длинные тексты в полях
- Специальные символы в названиях

#### 11.3. Тестирование производительности

- Большое количество записей (1000+)
- Длительное выполнение команд
- Запись длинного видео
- Утечки памяти

---

### Шаг 12: Финальные штрихи

#### 12.1. Иконка приложения

1. Создайте или скачайте .ico файл
2. Добавьте в проект
3. В .csproj укажите:
```xml
<PropertyGroup>
  <ApplicationIcon>app_icon.ico</ApplicationIcon>
</PropertyGroup>
```

#### 12.2. Информация о приложении

В .csproj добавьте метаданные:
```xml
<PropertyGroup>
  <AssemblyName>SpiderCrudApp</AssemblyName>
  <Product>Spider CRUD Application</Product>
  <Version>1.0.0</Version>
  <Company>Your Name</Company>
  <Copyright>Copyright © 2025</Copyright>
</PropertyGroup>
```

#### 12.3. Настройки приложения

Создайте файл конфигурации:
- appsettings.json для настроек
- Settings класс для хранения пользовательских настроек
- Путь к БД, темная тема, языки и т.д.

#### 12.4. Документация

Создайте документацию для пользователей:
- Руководство пользователя
- FAQ
- Скриншоты интерфейса

---

## 📦 Структура финального проекта

```
SpiderCrudApp/
├── Models/
│   ├── Category.cs
│   ├── CategoryData.cs
│   ├── Command.cs
│   ├── DockerProject.cs
│   └── DockerImage.cs
├── ViewModels/
│   ├── BaseViewModel.cs
│   ├── MainViewModel.cs
│   ├── CategoriesViewModel.cs
│   ├── CommandsViewModel.cs
│   ├── DockerViewModel.cs
│   └── ScreenCaptureViewModel.cs
├── Views/
│   ├── CategoriesView.xaml
│   ├── CommandsView.xaml
│   ├── DockerView.xaml
│   ├── ScreenCaptureView.xaml
│   └── Dialogs/
│       ├── CategoryDialog.xaml
│       ├── CommandDialog.xaml
│       └── ProjectDialog.xaml
├── Services/
│   ├── IDataService.cs
│   ├── DataService.cs
│   ├── CommandExecutor.cs
│   ├── DockerService.cs
│   ├── ScreenCaptureService.cs
│   └── VideoRecorderService.cs
├── Data/
│   ├── DatabaseContext.cs
│   ├── IRepository.cs
│   └── Repository.cs
├── Helpers/
│   ├── RelayCommand.cs
│   ├── ObservableObject.cs
│   └── ValidationHelper.cs
├── Resources/
│   ├── Styles/
│   │   ├── Buttons.xaml
│   │   ├── ListBoxes.xaml
│   │   └── DataGrids.xaml
│   ├── Images/
│   └── Icons/
├── App.xaml
├── App.xaml.cs
├── MainWindow.xaml
├── MainWindow.xaml.cs
└── SpiderCrudApp.csproj
```

---

## 🔧 Рекомендуемые инструменты и библиотеки

### Обязательные:
- **Visual Studio 2022** - IDE
- **.NET 6.0+** - Framework
- **Entity Framework Core** - ORM для работы с БД
- **SQLite** - База данных

### UI/UX:
- **MaterialDesignThemes** - Material Design стили
- **ModernWpfUI** - Modern UI стили
- **Hardcodet.NotifyIcon.Wpf** - Иконка в трее (опционально)

### Функциональность:
- **SharpAvi** - Запись видео
- **System.Drawing.Common** - Работа с изображениями
- **Serilog** - Логирование
- **Newtonsoft.Json** - Работа с JSON (если нужно)

### Вспомогательные:
- **CommunityToolkit.Mvvm** - Упрощение MVVM
- **FluentValidation** - Валидация данных

---

## ⚠️ Важные замечания

### Безопасность:
1. **Выполнение команд**: Валидируйте команды перед выполнением
2. **Пути файлов**: Проверяйте существование и права доступа
3. **Docker операции**: Обрабатывайте ошибки доступа

### Производительность:
1. Используйте асинхронность для долгих операций
2. Не блокируйте UI поток
3. Оптимизируйте запросы к БД

### Пользовательский опыт:
1. Показывайте индикаторы загрузки
2. Подтверждайте удаление данных
3. Сохраняйте состояние приложения
4. Обрабатывайте все ошибки

---

## 📝 Чеклист разработки

### Подготовка:
- [ ] Установлена Visual Studio 2022
- [ ] Создан проект WPF
- [ ] Создана структура папок
- [ ] Установлены необходимые NuGet пакеты

### Вкладка 1 - Категории:
- [ ] Модели данных созданы
- [ ] CRUD операции для категорий
- [ ] CRUD операции для данных категорий
- [ ] UI спроектирован и реализован
- [ ] Data binding настроен

### Вкладка 2 - Команды:
- [ ] Модель команды создана
- [ ] CRUD операции для команд
- [ ] Выполнение команд с аргументами
- [ ] Отображение логов в реальном времени
- [ ] UI спроектирован и реализован

### Вкладка 3 - Docker:
- [ ] Модели проекта и образов созданы
- [ ] CRUD операции для проектов
- [ ] Интеграция с Docker Compose
- [ ] Управление образами (старт, стоп, rebuild)
- [ ] UI спроектирован и реализован

### Вкладка 4 - Скриншоты/Видео:
- [ ] Функционал выбора области экрана
- [ ] Захват скриншота
- [ ] Редактор скриншотов (рисование, текст)
- [ ] Сохранение скриншотов
- [ ] Захват и запись видео
- [ ] UI спроектирован и реализован

### Финализация:
- [ ] Обработка ошибок
- [ ] Логирование
- [ ] Тестирование всех функций
- [ ] Оптимизация производительности
- [ ] Сборка в один EXE
- [ ] Тестирование финальной сборки
- [ ] Документация

---

## 🚀 Команды для сборки

### Режим разработки:
```powershell
dotnet build
dotnet run
```

### Релизная сборка (один EXE):
```powershell
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ./publish
```

### С оптимизацией размера:
```powershell
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:PublishTrimmed=true -o ./publish
```

---

## 📞 Поддержка и развитие

### Возможные улучшения:
1. Экспорт/импорт данных (JSON, Excel)
2. Темная тема
3. Мультиязычность
4. Резервное копирование БД
5. Горячие клавиши
6. Поиск и фильтрация
7. История изменений
8. Синхронизация с облаком

### Известные ограничения:
- Docker должен быть установлен для работы 3-й вкладки
- Требуются права администратора для некоторых команд
- Видеозапись может нагружать CPU

---

## 📄 Лицензия

Этот проект создается для личного использования.

---

Удачи в разработке! 🎉


